Exercise 2:
Maintain a History of Messages encrypted/decrypted.
    -Button to navigate to history page.
    -Whenever a message is en/decrypted -> add to a persistent store.
History page:
    ScrollView:
    Each element contains:
    -Original messages | shift key | result.
        -Button that allows user to delete this item from the persisted history.
            -Confirmation button to do this => confirm that they really wanna delete item
            -Include a warning that it is permanent.
            *** Will be limited to testing on the mobile edition.